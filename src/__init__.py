# src/__init__.py
# Makes `src` a proper Python package so modules can be imported as:
#   from src.model import TransformerBlock, PositionalEmbedding
#   from src.inference_engine import SignLanguageInferenceEngine
#   from src.data_pipeline import LandmarkExtractor, FeatureBuilder, SequenceBuffer
#   from src.speech_engine import SpeechEngine
